{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Hero_2027 Debug",
            "type": "shell",
            "command": "cmake --build build/Debug --parallel 4",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$gcc"
            ]
        }
    ]
}
